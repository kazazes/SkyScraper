type Transcription {
    id: ID! @id
    call: TrunkedCall!
    languageModel: String!
    beta: Float!
    body: String!
    words: [TranscriptionWord]! @relation(name: "WordsOnTranscription" onDelete: CASCADE)
    duration: Float!
    alpha: Float!
}

type TranscriptionWord {
    id: ID! @id
    text: String!
    transcription: Transcription! @relation(name: "WordsOnTranscription")
    confidence: Float!
    end: Int!
    start: Int!
}
