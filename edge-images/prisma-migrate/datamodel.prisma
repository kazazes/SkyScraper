interface IApplication {
  name: String!
  containerName: String!
  depends: [IApplication]!
  up: Boolean! @default(value: false)
  configurations: [IApplicationConfig]!
  activeConfiguration: IApplicationConfig
}

interface IApplicationConfig {
  application: IApplication!
  name: String! @unique
}
